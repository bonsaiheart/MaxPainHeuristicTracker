if market is open, run script

for date in index, if date has passed, and if date+1 = a column , populate that square with (close)$$$380


# Stuff to help me remember what I wanted to do with Jupyter and styler.
# def rain_condition(v):
#     if v < 1.75:
# #         return "Dry"
#     elif v < 2.75:
#         return "Rain"
#     return "Heavy Rain"
#
# def make_pretty(styler):
#     styler.set_caption("Weather Conditions")
#     styler.format(rain_condition)
#     styler.format_index(lambda v: v.strftime("%A"))
#     styler.background_gradient(axis=None, vmin=1, vmax=5, cmap="YlGnBu")
#     return styler
#
# bigframe.style.pipe(make_pretty)
